{
  "name": "koi-garden",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "assets:build": "node scripts/build-atlas-assets.js",
    "assets:pack-loose": "node scripts/pack-loose-assets.js",
    "predev": "npm run assets:build",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run assets:build",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pixi.js": "^8.13.2"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "lamejs": "^1.2.1",
    "sharp": "^0.35.3",
    "vite": "^7.0.0"
  }
}
